# Base URL of the deployed SupplySense FastAPI backend (Render/Railway).
# Leave unset for local dev -- vite.config.js proxies /api to http://localhost:8000.
VITE_API_BASE_URL=https://your-backend.onrender.com
